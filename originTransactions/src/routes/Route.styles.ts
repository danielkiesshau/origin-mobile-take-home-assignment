import {palette} from '@theme/palette';

const DEFAULT_HEADER_STYLES = {
  headerTintColor: palette.PRIMARY_COLOR,
  headerTitleStyle: {color: palette.BLACK_1},
};

export default {
  DEFAULT_HEADER_STYLES,
};
