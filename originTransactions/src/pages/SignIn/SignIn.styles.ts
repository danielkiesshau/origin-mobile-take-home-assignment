import styled from 'styled-components/native';
import ButtonNative from '@components/Button/Button.native';

const ActionButton = styled(ButtonNative)`
  margin-vertical: 8px;
`;

export default {ActionButton};
